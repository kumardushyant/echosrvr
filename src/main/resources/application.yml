management:
  server:
    port: 1081
  endpoints:
    web:
      path-mapping:
        prometheus: metrics
      base-path: /
      exposure:
        include: "prometheus,health,info"
server:
  port: 8080
